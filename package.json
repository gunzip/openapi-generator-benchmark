{
  "name": "openapi-generator-benchmark",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "^1.0.0"
  },
  "dependencies": {
    "typescript": "^5.4.5"
  }
}
