{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {
      "dependsOn": [],
      "inputs": ["openapi/**/*", "packages/**/*"],
      "outputs": ["{workspace}/dist/**/*"],
      "cache": true,
      "persistent": true
    }
  }
}
